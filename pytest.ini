[pytest]
# Python 경로 설정 (프로젝트 루트를 PYTHONPATH에 추가)
pythonpath = .

# 테스트 파일이 위치한 디렉토리
testpaths = tests

# 테스트 파일 패턴
python_files = test_*.py

# 테스트 클래스 패턴
python_classes = Test*

# 테스트 함수 패턴
python_functions = test_*

# 출력 옵션
addopts = -vv --tb=short
